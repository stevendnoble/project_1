<!DOCTYPE html>
<html lang="en">
<head>
{{> headCDN}}
	<title>Profile</title>
</head>
<body>
	{{> navPartial}}
	<h1 class='head-h1'>Helloo {{user.username}}</h1>

	{{#each user.events}}
        <div class="col-md-3">
          <div class="thumbnail">
            <img src="{{imgurUrl}}" alt='default-img' class='img-responsive profile-pic'>
            <hr>
            <h3>{{title}}</h3>
            <p><strong>When: </strong>{{startTime}}</p>
            <p><strong>Where: </strong>{{placeName}}</p>
            <hr>
            <p><a href="{{url}}" class="btn btn-sm btn-primary">More Info</a>
            <a href="#" class="btn btn-sm btn-success  fav-event" id="{{id}}">Remove</a></p>
            <hr>
          </div>
        </div>
        {{/each}}
<section>
  <div id='map'></div> 
</section>
{{> endCDN}}
</body>
</html>